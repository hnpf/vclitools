#!/bin/bash
# Description: Pretty-prints $PATH and flags broken directories.
IFS=':' read -ra ADDR <<< "$PATH"
for i in "${ADDR[@]}"; do
    if [ -d "$i" ]; then
        echo -e "\e[32m✔\e[0m $i"
    else
        echo -e "\e[31m✘\e[0m $i (not found)"
    fi
done
